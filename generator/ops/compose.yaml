services:
  cv:
    build:
      dockerfile: ops/Dockerfile
      context: ..
    volumes:
      - ../app:/app
      - ../data:/data
    networks:
      - cv
    ports:
      - ${HTTP_PORT:-8456}:8000

  exporter:
    image: mcr.microsoft.com/playwright:v1.50.0-jammy
    command: sh -c "npm install --prefix /script --omit=dev && node /script/server.js"
    environment:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
    volumes:
      - ../exporter:/script
    networks:
      - cv
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 2s
      timeout: 3s
      retries: 10
      start_period: 5s

networks:
  cv:
